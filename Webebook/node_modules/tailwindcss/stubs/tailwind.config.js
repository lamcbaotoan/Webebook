/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class', // Thêm dòng này
    content: [
        "./WebForm/Admin/**/*.aspx",
        "./WebForm/Admin/**/*.master",
    ],
    theme: {
        extend: {
            // Bạn có thể thêm màu sắc tùy chỉnh cho dark mode ở đây nếu muốn
        },
    },
    plugins: [],
}